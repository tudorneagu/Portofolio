version: "3"
services:
  Portofolio:
    image: nginx
    container_name: portofolio
    volumes:
      - ./portofolio:/usr/share/nginx/html
    expose:
      - "80"
    networks:
      - nginx-proxy
    environment:
      VIRTUAL_HOST: tudorneagu.com
      VIRTUAL_PORT: 80
      LETSENCRYPT_HOST: tudorneagu.com
      LETSENCRYPT_EMAIL: neagu.t@gmail.com

networks:
  nginx-proxy:
    external: true
